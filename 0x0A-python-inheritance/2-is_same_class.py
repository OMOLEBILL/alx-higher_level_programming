#!/usr/bin/python3
"""Checking the exact istances
"""


def is_same_class(obj, a_class):
    """check if an obj is an istance of a class
    """
    if type(obj) is a_class:
        return True
    return False
